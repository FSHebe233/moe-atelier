version: "3.8"

services:
  app:
    build: .
    ports:
      - "5173:5173"
    environment:
      - PORT=5173
    volumes:
      - saved-images:/app/saved-images
      - server-data:/app/server-data
    restart: unless-stopped

volumes:
  saved-images:
  server-data:
